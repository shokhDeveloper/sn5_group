// Github nima ?
// Git

class Car {
    constructor(carname, caryear){
        this.carname = carname;
        this.caryear = caryear
    }
};

let cobalt = new Car('Cobalt', 2025);
console.log(cobalt)

// main (local)
// main (global)